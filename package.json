{
	"name": "zamock",
	"version": "1.0.2",
	"description": "batch mock ajax ",
	"main": "index.js",
	"directories": {
		"test": "test"
	},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Carry",
	"license": "ISC",
	"dependencies": {
		"xhr2": "^0.1.3"
	},
	"devDependencies": {
		"xhr2": "^0.1.3"
	}
}